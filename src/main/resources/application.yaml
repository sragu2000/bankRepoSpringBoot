server:
  port:
    8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/my_bank?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: Password123#@!
    driverClassName: com.mysql.cj.jdbc.Driver
  application:
      name: bank-service
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update